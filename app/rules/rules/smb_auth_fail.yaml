id: R7003
name: SMB authentication failures
description: Multiple failed SMB authentication attempts from a single host
summary: Detects repeated SMB authentication failures from the same host, which may indicate credential guessing, brute-force attempts, or probing of network file-sharing services.
enabled: true
conditions:
  event_type: smb_auth_failure
window:
  minutes: 2
threshold:
  count: 8
severity: medium
key_field: src_host
mitre_attack: TA0008